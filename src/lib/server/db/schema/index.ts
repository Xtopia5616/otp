// src/lib/server/db/schema/index.ts — Drizzle schema barrel (Engineering §8.1)
// Stage 0 占位：schema 表定义在 Stage 4（Architecture §4）落地。
// 此文件保证 drizzle.config.ts 的 schema 路径可解析、`drizzle-kit push` 能连通。
//
// Stage 4 起将在此汇总导出 user / vault / passkeyWrap 三表 schema：
//   export * from './user';
//   export * from './vault';
//   export * from './passkey-wrap';
export {};
